# 最终优化建议 - 目标1000-2000个样本

## 📊 分析结论

**当前优化后的条件**可能产生**~24,190个样本**，远超过目标的1,000-2,000个。

**原因**：
- 25年数据量巨大（约2800万窗口）
- 即使通过率很低（0.085%），样本数也会很多
- 需要适度收紧条件

---

## 🎯 推荐方案

### 方案1：适度收紧（推荐）⭐

**目标**: 产生1,000-2,000个高质量样本

**配置调整**：
```yaml
future_return_threshold: 50   # 恢复到50%（提高质量）
volume_ratio_min: 1.5         # 从1.2提高到1.5
volume_ratio_max: 3.0         # 从4.0降低到3.0
min_signals: 2                # 保持2个信号
```

**预期效果**：
- 通过率: ~0.034%（接近原始值）
- 预期样本数: ~9,600个
- 仍可能略多，但质量最高

### 方案2：分阶段筛选（灵活）⭐

**第一阶段**：使用较严格条件筛选
```yaml
future_return_threshold: 50
volume_ratio_min: 1.5
volume_ratio_max: 3.0
min_signals: 2
```

**第二阶段**：在结果中精选
- 按未来涨幅排序，选择Top 10-20%
- 或按综合质量评分筛选

**优势**：
- 保留所有高质量样本
- 可以根据需要灵活调整数量
- 便于分析不同质量层次

### 方案3：动态调整（最灵活）

**策略**：
1. 先使用方案1运行一次
2. 查看实际样本数量
3. 如果样本在1,000-2,000范围内 → 完美
4. 如果样本过多 → 进一步收紧或精选
5. 如果样本不足 → 适当放宽

---

## 📋 具体执行建议

### 推荐执行步骤

#### 步骤1：使用方案1配置

修改`config/settings.yaml`：
```yaml
left_breakout:
  sample_preparation:
    positive_criteria:
      future_return_threshold: 50   # 恢复到50%
      past_return_threshold: 20
      rsi_threshold: 70
      volume_ratio_min: 1.5          # 恢复到1.5
      volume_ratio_max: 3.0          # 恢复到3.0
      min_signals: 2
```

#### 步骤2：运行筛选

```bash
python scripts/prepare_left_breakout_samples.py
```

#### 步骤3：查看结果

- 如果样本数在1,000-2,000 → ✅ 完美
- 如果样本数 > 2,000 → 考虑精选或进一步收紧
- 如果样本数 < 1,000 → 适当放宽条件

---

## 🔄 如果样本仍然过多

### 选项A：进一步收紧条件

```yaml
future_return_threshold: 55   # 提高到55%
volume_ratio_min: 1.8         # 提高到1.8
volume_ratio_max: 2.5         # 降低到2.5
min_signals: 2
```

### 选项B：在结果中精选

在筛选结果中：
- 按未来涨幅排序，选择Top 10-15%
- 或按综合质量评分筛选
- 或按时间分布均匀选择

---

## 📊 预期对比表

| 方案 | 未来涨幅 | 量比范围 | 预期样本数 | 质量 | 推荐度 |
|------|---------|---------|-----------|------|--------|
| 当前优化 | 40% | 1.2-4.0 | ~24,190 | 中 | ❌ 过多 |
| **方案1（推荐）** | **50%** | **1.5-3.0** | **~9,600** | **高** | **⭐⭐⭐** |
| 方案2（精选） | 50% | 1.5-3.0 | ~9,600→1,920 | 很高 | ⭐⭐ |
| 进一步收紧 | 55% | 1.8-2.5 | ~5,000 | 很高 | ⭐ |

---

## ✅ 最终建议

**推荐使用方案1**：
1. 恢复到较严格的条件（50%涨幅，1.5-3.0量比）
2. 运行筛选，查看实际样本数
3. 如果样本在1,000-2,000范围内 → 完美
4. 如果样本仍然过多 → 在结果中精选Top 10-20%

**理由**：
- 25年数据量巨大，即使严格条件也会产生较多样本
- 先筛选出高质量样本，再根据需要精选
- 灵活可控，便于调整

---

**请确认是否执行方案1？**

