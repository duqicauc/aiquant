# 左侧潜力牛股筛选条件 - 已恢复到严格标准

## ✅ 配置更新

### 恢复的严格条件

```yaml
positive_criteria:
  future_return_threshold: 50   # 未来45天累计涨幅 > 50%（严格标准）
  past_return_threshold: 20     # 过去60天累计涨幅 < 20%（底部震荡）
  rsi_threshold: 70             # RSI < 70（未过度超买）
  volume_ratio_min: 1.5         # 量比 >= 1.5（严格标准）
  volume_ratio_max: 3.0         # 量比 <= 3.0（严格标准，温和放量）
  min_signals: 2                # 至少2个预转信号
```

### 对比

| 条件 | 优化后（宽松） | 严格条件（当前） |
|------|--------------|----------------|
| 未来涨幅 | 40% | **50%** ✅ |
| 量比范围 | 1.2-4.0 | **1.5-3.0** ✅ |
| 预转信号 | 2个 | **2个** ✅ |

---

## 📊 预期效果

### 基于诊断结果估算

**严格条件**（50%涨幅，1.5-3.0量比）：
- 条件1通过率: 2.40%（未来涨幅>50%）
- 条件4通过率: 5.14%（量比1.5-3.0）
- 理论通过率: 0.015%
- **预期样本数: ~4,500个**（在2800万窗口中）

**说明**：
- 25年数据量巨大，即使严格条件也会产生较多样本
- 如果样本仍然过多，可以在结果中精选Top 10-20%

---

## 🎯 优势

### 严格条件的优势

1. **样本质量高**：
   - 未来45天涨幅>50%是真正的潜力牛股
   - 量比1.5-3.0确保温和放量，不是异常波动

2. **符合左侧投资理念**：
   - 底部震荡（过去涨幅<20%）
   - 预转信号（至少2个技术信号）
   - 未来大涨（涨幅>50%）

3. **便于后续精选**：
   - 如果样本过多，可以按涨幅排序选择Top样本
   - 如果样本不足，可以适当放宽条件

---

## 📋 下一步

### 1. 重启脚本（使用新配置）

当前运行的脚本使用的是旧配置（40%涨幅，1.2-4.0量比），需要重启才能使用严格条件：

```bash
# 停止当前运行（如果需要）
# 然后重新运行
python scripts/prepare_left_breakout_samples.py
```

### 2. 监控进度

使用监控脚本查看进度：
```bash
bash scripts/monitor_left_breakout_progress.sh
```

### 3. 查看结果

运行完成后，检查样本数量：
- 如果样本在1,000-2,000范围内 → ✅ 完美
- 如果样本过多 → 可以在结果中精选
- 如果样本不足 → 可以适当放宽条件

---

## ✅ 配置已更新

**文件**: `config/settings.yaml`

**状态**: ✅ 已恢复到严格条件

**生效**: 需要重启脚本才能使用新配置

---

**配置已恢复！可以重启脚本使用严格条件了。** 🎯

