# AIQuant 量化交易系统 - 依赖包列表
# 安装命令: pip install -r requirements.txt
# 如果只需要核心功能（无回测/Web/可视化面板）: pip install -r requirements.txt --no-deps

# ============================================================================
# 核心依赖（必需）
# ============================================================================
pandas>=2.0.0
numpy>=1.24.0
scipy>=1.11.0

# 数据获取
tushare>=1.4.0
akshare>=1.11.0
yfinance>=0.2.0

# 数据库
sqlalchemy>=2.0.0

# 工具库
joblib>=1.3.0
tqdm>=4.66.0
loguru>=0.7.0
python-dotenv>=1.0.0
pyyaml>=6.0.0
click>=8.1.0
requests>=2.31.0

# ============================================================================
# 机器学习（核心算法必需）
# ============================================================================
scikit-learn>=1.3.0
xgboost>=2.0.0

# ============================================================================
# 技术指标（特征工程必需）
# ============================================================================
ta-lib>=0.4.28
pandas-ta>=0.3.14

# ============================================================================
# 可选依赖 - 回测框架（如不需要回测功能可省略）
# ============================================================================
backtrader>=1.9.78         # 事件驱动回测
vectorbt>=0.26.0           # 向量化回测验证

# ============================================================================
# 可选依赖 - 其他ML库（目前主模型使用XGBoost，以下备用）
# ============================================================================
lightgbm>=4.0.0            # 备用梯度提升框架
catboost>=1.2.0            # 备用梯度提升框架

# ============================================================================
# 可选依赖 - 深度学习（当前未启用，未来扩展）
# ============================================================================
# torch>=2.0.0
# tensorflow>=2.13.0

# ============================================================================
# 可选依赖 - 数据存储增强（如不需要可省略）
# ============================================================================
h5py>=3.9.0                # HDF5格式支持
pyarrow>=13.0.0            # Parquet格式支持
pymysql>=1.1.0             # MySQL支持
psycopg2-binary>=2.9.0     # PostgreSQL支持
redis>=5.0.0               # Redis缓存（可选）

# ============================================================================
# 可选依赖 - 可视化面板（如不需要Web界面可省略）
# ============================================================================
streamlit>=1.28.0          # Web可视化面板
plotly>=5.17.0             # 交互式图表

# ============================================================================
# 可选依赖 - 传统可视化（基础图表已包含matplotlib）
# ============================================================================
matplotlib>=3.7.0
seaborn>=0.12.0
mplfinance>=0.12.10        # K线图
pyecharts>=2.0.0           # ECharts图表（如使用app.py中的部分图表需安装）

# ============================================================================
# 可选依赖 - 任务调度（自动化系统使用）
# ============================================================================
schedule>=1.2.0

# ============================================================================
# 开发/测试依赖
# ============================================================================
pytest>=7.4.0
pytest-cov>=4.1.0
pytest-timeout>=2.2.0      # 测试超时（pyproject.toml中配置了timeout）

# ============================================================================
# Jupyter（数据分析/实验使用）
# ============================================================================
jupyter>=1.0.0
ipykernel>=6.25.0
