# AIQuant 量化交易系统 - 依赖包列表
# 安装命令: pip install -r requirements.txt
#
# ⚠️  部署新机器前，先在当前环境运行以下命令生成此文件：
#     pip list --not-required --format=freeze | grep "==" > requirements.txt
#     （需先激活 venv：source venv/bin/activate）
#
# 本文件由 venv 实际安装包 freeze 生成，精确锁定版本。
# 所有传递依赖由 pip 自动解析，无需列入。

# ============================================================================
# 核心依赖（必需）
# ============================================================================
pandas==2.3.3
numpy==2.2.6

# 数据获取
tushare==1.4.29
akshare==1.18.63
yfinance==1.4.0

# 数据库 / 数据存储
arcticdb==6.17.0                      # ⚠️ 核心依赖：选股中心/每日流水线必需（LMDB 后端）
msgpack==1.1.2                        # arcticdb 传递依赖，需同步安装
sqlalchemy==2.0.39

# 工具库
joblib==1.5.1
tqdm==4.67.1
loguru==0.7.3
python-dotenv==1.0.1
pyyaml==6.0.3
click==8.1.8
requests==2.32.4

# ============================================================================
# 机器学习（核心算法必需）
# ============================================================================
scikit-learn==1.7.1
xgboost==3.2.0
lightgbm==4.6.0
catboost==1.2.10

# 技术指标
TA-Lib==0.6.8
pandas-ta==0.4.71b0

# ============================================================================
# 回测框架
# ============================================================================
backtrader==1.9.78.123
vectorbt==1.0.0
h5py==3.16.0

# ============================================================================
# 可视化
# ============================================================================
matplotlib==3.10.1
seaborn==0.13.2
mplfinance==0.12.10b0
pyecharts==2.1.0

# ============================================================================
# Web API / 任务调度（自动化系统必需）
# ============================================================================
fastapi==0.136.3                      # ⚠️ 核心依赖：选股中心 API 后端
uvicorn==0.35.2
APScheduler==3.11.2

# ============================================================================
# 认证 / 安全
# ============================================================================
bcrypt==5.0.0
passlib==1.7.4
python-jose==3.5.0

# ============================================================================
# 前端 / 交互
# ============================================================================
streamlit==1.57.0
agentscope==1.0.21                    # ⚠️ 核心依赖：AgentScope 多智能体框架

# ============================================================================
# Jupyter / 开发工具
# ============================================================================
jupyter==1.1.1
ipykernel==6.25.2
pytest==8.4.2
pytest-cov==7.1.0
pytest-timeout==2.4.0

# ============================================================================
# 传递依赖（自动解析，无需手动管理）
# ============================================================================
# 以下为 pip 自动解析的传递依赖，锁版本由上面直接依赖决定。
# 禁止手动删除或修改以下包，否则可能导致兼容性问题。
psycopg2-binary==2.9.12
pymysql==1.2.0
redis==7.4.0
pyarrow==19.0.1
pydantic==2.12.2

# 传递依赖（fastapi / starlette / jsonschema 等连带）
fqdn==1.5.1
isoduration==20.11.0
jsonpointer==3.1.1
rfc3987-syntax==1.1.0
tinycss2==1.4.0
uri-template==1.3.0
webcolors==25.10.0